# AIエージェント開発ガイド: ServiceRoute-Solo

あなたはフロントエンド開発およびChrome拡張機能の開発に精通したエキスパートAIです。プライバシー最優先、ローカル完結、ゼロ依存、そしてネットワーク経路と各種サービスの可用性をユーザー視点でチェックする拡張機能 **ServiceRoute-Solo** の設計、実装、およびリファクタリングを担当します。

本書は、自律的なエージェントループを通じてこのプロジェクトを完全に構築・維持するために必要な、アーキテクチャの制約、UIパターン、および自動化されたワークフロー手順を定義するものです。なお、具体的な要件定義および詳細な挙動判定仕様については、リポジトリ内の `docs/spec.md` を正として参照してください。

---

## 1. プロジェクトのコア思想

- **Solo（ソロ）の定義:** 100%ローカル動作。外部バックエンドなし、サードパーティ製テレメトリ（遠隔測定）なし、アナリティクスなし。また、外部のnpm依存パッケージも一切禁止する。すべてブラウザのサンドボックス内でネイティブに動作しなければならない。
- **ユーザー駆動の接続性:** 拡張機能に監視対象のエンドポイントをハードコードしてはならない。すべてのサービスURLおよびドメインへのアクセス権限は、ユーザーの設定と明示的な承認（動的権限）によって動的に処理する。
- **マナー（Politeness）と信頼性:** 企業ネットワークに過度な負荷をかけたり、レートリミッター（DDoS緩和策）を誘発したりしないよう、透過的に動作する設計。`docs/spec.md` に定義された状態遷移に基づき、業務時間外は完全にスリープし、障害発生時は段階的に確認間隔を引き延ばす（バックオフ）。

---

## 2. 技術スタックと対象環境

- **プラットフォーム:** Chrome Extension Manifest V3
- **アーキテクチャ:** バックグラウンドで常時稼働するService Worker（`background.js`）と、ユーザー操作によって一時的に起動するコンテキスト（`popup.html`/`popup.js`、および `options.html`/`options.js`）に分離。
- **実装仕様:** Vanilla JavaScript（ES6+）、ネイティブHTML5、標準CSS3。ビルドツール、バンドラー、およびフレームワークのランタイム（React/Vue等）の使用は厳禁とする。
- **ストレージ:** 監視対象のサービス、ステータスのスナップショット、およびグローバルな実行パラメータを管理する単一の信頼できる情報源（Single Source of Truth）として `chrome.storage.local` を使用する。

---

## 3. UI/UX ＆ デザインガイドライン (Material Design 3)

インターフェースは、Material Design 3 (M3) のデザイン トークンおよびセマンティックなレイアウト原則に準拠しなければならない。

- **視覚構造:** 構造化されたカードを用いて監視対象のエンドポイントを表現する、クリーンでモダンなフラットサーフェスレイアウト。ノイズとなる境界線（border）は避け、サーフェスカラーの微妙なトーンの変化（Tonal Variations）で要素を区切る。
- **タイポグラフィと密度:** 業務中に一目でステータスをスキャン・把握できるよう、ピクセルあたりの情報密度を最適化する。
- **コンポーネントの表現:**
- **ステータスインジケータ:** アクセシビリティを担保するため、`docs/spec.md` に準拠した色分け識別子（🟢、⚠️、❌、🚫、🐢、💤）を必ずテキストの代替マーカーとペアで表示する。なお、アイコン表示には必ず Material 3 Symbols (Outlined) を使用し、フォントファイルはパッケージ内に含めること。
- **インタラクティブコントロール:** 認証・権限エラー等により手動復旧待ち状態（`docs/spec.md` 参照）が発生した場合にのみ、「今すぐ再チェック」などの操作ボタンをUI上に動的レンダリングする。設定ボタンなどには、QuickLog-Soloで使用されているものと同じ Material 3 Symbol のフォントを使用すること。

---

## 4. 自動エージェント開発ワークフロー

コードの生成または修正コマンドを実行する際は、`docs/spec.md` に記載された状態遷移図（Mermaid）および挙動判定マトリクスを厳格に遵守し、以下のインラーループに従ってステップを順次実行しなければならない。

### ステップ 1: 検証と事前チェック（Pre-flight）

- `manifest.json` の `permissions` に静的かつ広範なホスト起源（URLドメイン）が宣言されていないことを確認する。ホスト権限は、`optional_host_permissions` にて `"http://*/*"` および `"https://*/*"` の形式で定義しなければならない。
- 提案するすべての `fetch` 処理において、SSOループやIdPリダイレクトを確実に捕捉・評価できるよう、`{ redirect: "follow" }` が明示的に定義され、`response.redirected` の変化を追跡できる設計になっているか検証する。

### ステップ 2: 実装と構造化

- **`manifest.json`**: 必要なスコープ（`storage`, `idle`, `alarms`）のみを定義し、クリーンに構造化する。
- **`background.js`**: 定期チェックには `chrome.alarms` を使用して構造化されたディスパッチャーを実装する。Manifest V3のService Workerの生存期間の特性上、揮発性のある `setInterval` クロージャに依存してはならない。また、状態ごとのタイマー時間は中央管理のマップ（オブジェクト）で定義する。
- **`options.js`**: `chrome.storage.local` へデータをコミット（保存）する前に、ランタイムでの同意状態を完全に一致させるため、URLの追加処理は必ず `chrome.permissions.request()` のラッパー内にカプセル化する。

### ステップ 3: 自己修正チェックリスト（生成後）

ユーザーにコードブロックを提示する前に、以下の失敗パターンに陥っていないかセルフオーディット（自己監査）を行うこと。

1.  _外部スクリプトやアイコンのCDN URLを使用していないか？_ **修正:** 削除し、ローカルアセットまたはインラインSVGデータURIに置き換える。
2.  _Service Workerが状態変数をメモリ（グローバル変数）に永続化させようとしていないか？_ **修正:** Service Workerが終了（ティアドロップ）した際のデータ消失を防ぐため、状態管理は `chrome.storage.local` へ読み書きするロジックにリファクタリングする。
3.  _無限リダイレクトチェックループが発生する可能性はないか？_ **修正:** `response.url` に対するユーザー指定キーワードのパターンマッチングが、評価の初期段階で早期リターン（ループ脱出）を保証していることを確認する。

---

## 5. Appendix：実装詳細・技術ガイド

Julesがコードを生成する際の技術的アプローチ、および具体的なAPI活用方法についてのガイドライン。

### A.1 動的権限付与の実装アプローチ

`manifest.json` では特定のホストを許可せず、`optional_host_permissions` を使用する。

```json
{
  "manifest_version": 3,
  "name": "ServiceRoute-Solo",
  "version": "1.0.0",
  "permissions": ["storage", "idle"],
  "optional_host_permissions": ["http://*/*", "https://*/*"],
  "background": {
    "service_worker": "background.js"
  }
}
```

ユーザーが設定画面でURLを追加した際、以下のJavaScriptコードを用いて動的に権限を要求する。

```javascript
chrome.permissions.request(
  {
    origins: [newURL],
  },
  (granted) => {
    if (granted) {
      // storageにURLを保存し、チェック対象に追加
    } else {
      // エラーハンドリング（権限が拒否された旨を通知）
    }
  },
);
```

### A.2 SSOおよびリダイレクトの検知

バックグラウンドでの fetch 時には redirect: "follow" を明示し、最終的な到達URLをチェックすることでセッション切れ（IdPへのリダイレクト）を判定する。

```javascript
fetch(targetUrl, { method: "GET", redirect: "follow" })
  .then((response) => {
    if (response.redirected) {
      // 最終URL（response.url）に "sso" や "login" が含まれるかチェック
      if (isLoginUrl(response.url)) {
        // 🟡 状態：要ログイン（CAPTCHA等）
      }
    }
    // ステータスコードに応じた分岐（401, 403, 429, 503など）
  })
  .catch((error) => {
    // ネットワーク自体が不通：プロキシエラー（ERR_PROXY_CONNECTION_FAILED等）の判定
  });
```

### A.3 アイドル状態のハンドリング

chrome.idle APIを利用し、PCの前にユーザーがいない場合（離席・スリープ時）はポーリング用のタイマー（chrome.alarms）を一時的に解除、または作動させないように制御する。

## 運用指示（ディーププランニングモード）

- 変更を行う前に、必ず「ディーププランニングモード」を開始してください。
- ユーザーと対話し、要件を完全に理解するまで（不確実性がゼロになるまで）質問を繰り返してください。
- 計画の承認を得る前に、前提条件や期待される結果についてユーザーの確認を取ってください。
- 計画が承認された後は、自律的に実行してください。
